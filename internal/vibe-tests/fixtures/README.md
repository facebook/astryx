# Consumer fixture gallery

This deterministic app is the low-semantic control for setup testing. The screenshot documents its production build; it is not a golden test.

| Plain Tailwind control                                                                                                                |
| ------------------------------------------------------------------------------------------------------------------------------------- |
| ![Plain Tailwind v4 control in light mode](assets/tailwind-v4-control-light.png)                                                      |
| A minimal Tailwind app that catches setup steps that assume an existing semantic theme or change baseline utility and reset behavior. |

The gallery is capped at 250 KiB per PNG and 300 KiB total.
