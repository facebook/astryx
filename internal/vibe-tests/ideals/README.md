# Ideal Reference Images

Reference designs for the Night Watch Design dimension. These are the
"target" that generated screenshots are compared against.

## Naming Convention

```
{promptId}-{viewport}-{theme}.png   — specific (preferred)
{promptId}-{viewport}.png           — viewport-only (used for both themes)
{promptId}.png                      — generic fallback (used for all combos)
```

Examples:

- `cwm-1-desktop-light.png` — specific to desktop + light
- `ty-3-desktop.png` — desktop only, used for both light and dark
- `dd-3.png` — fallback for all viewport/theme combos

## Requirements

- **Format:** PNG
- **Viewport:** 1280×800 for desktop, 375×812 for mobile (matching screenshot-previews.ts)
- **Source:** Figma mockup or hand-built HTML — NOT generated by any of the 3 targets (XDS, baseline, HTML)
- **Review:** At least one designer should review the ideal before committing

## Current Ideals

_None yet — see [issue #733](https://github.com/facebookexperimental/xds/issues/733) for the selection process._

Recommended candidates (from the issue):

- `ty-3` — Metrics dashboard card
- `dd-3` — Product card grid
- `cwm-1` — Stripe pricing table
- `ps-1` — Settings dashboard
- `ty-8` — Profile card
