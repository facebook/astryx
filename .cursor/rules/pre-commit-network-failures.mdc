---
description: How to handle pre-commit hook failures caused by network issues
alwaysApply: true
---

# Pre-commit Hook Network Failures

When a `git commit` fails because the pre-commit hook (`npx lint-staged`, husky, etc.) cannot reach the npm registry (502, 503, ETIMEDOUT, EAI_AGAIN errors):

1. **Do not retry more than once.** The npm outage is transient and retrying wastes turns.
2. **Immediately ask the user** whether to skip the hook with `--no-verify`.
3. Explain that the hook runs ESLint + Prettier — if the code already passes `yarn lint` / `yarn format`, skipping is safe.

```bash
# If user approves:
git commit --no-verify -m "message"
```

Never spin on retries or attempt to install lint-staged locally as a workaround.
