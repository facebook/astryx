---
'@astryxdesign/core': patch
---

[feat] Markdown: add configurable entity references

Use `createMarkdownEntityReferencesPlugin()` to turn configured `@{id}` prose into accessible labels with optional caller rendering while preserving unknown and protected references.

@cixzhang
