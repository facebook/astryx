---
'@astryxdesign/cli': patch
---

[docs] The CLI README now lists every command's options and examples and every function in `@astryxdesign/cli/api` and `@astryxdesign/cli/json`, generated from the CLI's own docs. Entries that had drifted are fixed, such as the search flag that is `--verbose`, not `--detail`, and the config and integration fields the README left out.

@josephfarina
