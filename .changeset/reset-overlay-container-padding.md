---
'@astryxdesign/core': patch
---

[fix] Reset container padding custom properties on overlay elements (`BottomSheet`, `Dialog`, `MobileNav`, `Popover`) to prevent nested `Section` components from inheriting ancestor section padding.

@Geervan
