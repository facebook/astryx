---
'@astryxdesign/cli': patch
---

[fix] `astryx layout expand <expr> <path>` now labels its text fields `componentsUsed` and `todos`, the keys the `--json` output uses, instead of `Components` and `TODOs`.

@josephfarina
