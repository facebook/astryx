---
'@astryxdesign/cli': patch
---

[feat] Add ScrollableArea block templates so the component page has worked examples: a block-axis review queue showcase, a full-bleed inline card rail, sticky day headings pinned to the viewport edge, a two-axis build log nested in a scrolling panel with `overscroll="contain"`, a sticky pass-through queue pair contrasting a fitting viewport against `stickyContainment="always"`, and an order panel where the default `overscroll="allow"` chains a nested item list to the panel behind it.

@ernestt
