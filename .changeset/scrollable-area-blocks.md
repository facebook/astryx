---
'@astryxdesign/cli': patch
---

[feat] Add ScrollableArea block templates so the component page has worked examples: a block-axis review queue showcase, a full-bleed inline card rail, sticky day headings pinned to the viewport edge, and a two-axis build log nested in a scrolling panel with `overscroll="contain"`.

@ernestt
