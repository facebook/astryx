---
'@astryxdesign/core': patch
---

[fix] Name the no-search bottom-sheet `Selector` listbox from the component's `label` so Chromium exposes it with an accessible name; the searchable sheet and popover paths keep their existing trigger relationship. (#6395)

@kyu-rong
