---
'@astryxdesign/core': patch
---

[feat] expose the Dialog title/inline context via the @astryxdesign/core/Dialog/DialogContext subpath so composed headers outside core (lab's DialogHeroHeader) can join the aria-labelledby title handshake and inline-preview autofocus suppression, the same contract DialogHeader uses (#4182)
@jiunshinn
