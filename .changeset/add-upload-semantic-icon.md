---
'@astryxdesign/core': patch
---

[feat] Add an `upload` semantic icon with a distinct upload-to-tray fallback so upload actions no longer need to reuse the directional `arrowUp` meaning.

@rubyycheung
