---
'@astryxdesign/cli': patch
---

[fix] `astryx init` human output is now plain ASCII. Status lines use `[ok]` instead of a check glyph, and dashes, bullets and arrows print as `-` and `->`. `--json` output is unchanged.

@josephfarina
