---
'@astryxdesign/core': patch
---

[fix] EmptyState: the rest spread sits before the contract `role`, so a consumer can no longer clobber the landmark role the component guarantees (#4826).

@cixzhang
