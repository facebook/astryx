---
'@astryxdesign/cli': patch
---

[fix] The `themeBuild()` reference now documents every field of the `theme.build` receipt by name, including `notices`, the advisories (such as a font the theme names but does not load) that had no documentation.

@josephfarina
