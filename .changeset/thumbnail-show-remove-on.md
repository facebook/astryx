---
'@astryxdesign/core': patch
---

[feat] Thumbnail: add `showRemoveOn` prop — `'always'` (default) keeps today's behaviour, `'hover'` reveals the remove button on hover or keyboard focus (and keeps it visible on touch).

@kentonquatman
