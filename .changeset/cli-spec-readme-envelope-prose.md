---
'@astryxdesign/cli': patch
---

[docs] The CLI README now shows `apiVersion` in every hand-written envelope shape and example, and lists `astryx search --verbose` in place of `--detail`, which needs a level and does not add a result's score or reason. (#6587)

@josephfarina
