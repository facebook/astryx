---
'@astryxdesign/cli': patch
---

[feat] The grouped table template now sorts each account group on its own columns, runs all of them off one time range in the page header, and expands rows in place into a full-width history chart. Below 720px each table folds to its name and headline figure, with the move underneath as an arrow and a percentage.
@ernestt
