---
'@astryxdesign/cli': patch
---

[fix] `astryx manifest` without `--json` now labels each command's name `name:`, the same key the JSON manifest uses, instead of `command:`. (#6552)

@josephfarina
