---
'@astryxdesign/cli': patch
---

[fix] `astryx blog` text output now labels the feed URL `feedUrl`, matching its `--json` key, and prints every post field the JSON carries: the list adds each post's description, date, authors and link, and a post read adds its metadata above the body.

@josephfarina
