---
'@astryxdesign/core': patch
---

[fix] `Button` now applies the documented `--button-disabled-opacity` and `--button-press-scale` theme vars. Both values were previously hardcoded, so overriding these vars had no effect (#4423).

@cixzhang
