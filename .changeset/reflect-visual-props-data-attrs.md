---
'@xds/core': minor
'@xds/lab': minor
'@xds/cli': patch
---

Reflect component visual props and documented states as data attributes alongside the existing xds-\* and value classes. For example, buttons now render `data-variant` and `data-size` in addition to the existing compatibility classes. CLI component docs now show the reflected data attributes and recommend them for new external CSS selectors.
