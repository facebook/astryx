---
'@astryxdesign/cli': patch
---

[fix] `astryx doctor --help` now lists its exit codes from the command's own documentation, in plain ASCII, instead of a hand-written note that could drift from it and printed a non-ASCII dash.

@josephfarina
