---
'@astryxdesign/core': patch
---

[feat] Let `Button` keep `endContent` visible while `isIconOnly` hides its label, and let `DropdownMenu` show its chevron on label-hidden icon triggers.

@kentonquatman
