---
'@astryxdesign/cli': patch
---

[fix] The text output of `astryx discover` (the package list and a single package) now shows every field its `--json` entry carries, including `category` and `version`, which only the JSON used to include.

@josephfarina
