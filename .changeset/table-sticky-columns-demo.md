---
'@astryxdesign/cli': patch
---

[docs] Add a "Table — Sticky Columns" example block to the docsite, demonstrating
`useTableStickyColumns` with a column pinned to each edge (start + end) over a
horizontally-scrolling table. Complements the existing "Table — Resizable Columns"
example.
@humbertovirtudes
