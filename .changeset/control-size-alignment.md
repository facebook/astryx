---
'@astryxdesign/core': patch
---

[fix] CheckboxInput, RadioList, Switch: align control sizes. The visible checkbox and radio controls now fill their size exactly (20px at `sm`, 24px at `md`) instead of being inset 2px. The Switch thumb is now the same size across both variants, and the `sm` switch track is 32px wide.

@cixzhang
