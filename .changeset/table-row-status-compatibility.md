---
'@astryxdesign/core': patch
---

[feat] Add semantic Table row statuses while restoring every color value to the stable custom-marker behavior. Stable consumers need no migration; canary users relying on implicit glyphs should switch from `color` to `status`.

@cixzhang
