// Copyright (c) Meta Platforms, Inc. and affiliates.

/**
 * @file Shared building-block types used across the doc shapes (props, usage,
 * theming, anatomy, etc.). Re-exported from the canonical definitions in
 * ../docs-types. No factory - these are composed into the top-level doc kinds.
 *
 * Part of the `@astryxdesign/core/doc-types` single home for doc typing.
 */

import type {
  PropDoc,
  UsageDoc,
  ThemingTarget,
  DerivedVar,
  ComponentVar,
  AnatomyElement,
  BestPractice,
  ExampleDoc,
  ElementDescriptor,
  PlaygroundConfig,
} from '../docs-types';

export type {
  PropDoc,
  UsageDoc,
  ThemingTarget,
  DerivedVar,
  ComponentVar,
  AnatomyElement,
  BestPractice,
  ExampleDoc,
  ElementDescriptor,
  PlaygroundConfig,
};
