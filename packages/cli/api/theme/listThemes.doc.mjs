// Copyright (c) Meta Platforms, Inc. and affiliates.

/**
 * @file FunctionDoc for the `listThemes()` helper. Colocated with the theme
 * command's shared data layer (`_adapter.mjs`); the BundledTheme shape lives
 * there. This helper has no CLI command of its own — themeList and themeAdd
 * sit on it.
 * @position packages/cli/api/theme — function documentation
 */

/** @type {import('@astryxdesign/cli/authoring').FunctionDoc} */
export const doc = {
  type: 'function',
  kind: 'api',
  name: 'listThemes',
  displayName: 'listThemes()',
  summary: 'Read the bundled-theme manifest and return its raw entries.',
  description:
    "The shared data layer behind theme list/add: reads the CLI's bundled-theme manifest " +
    '(templates/themes/manifest.json, generated by scripts/generate-cli-themes.mjs) and returns ' +
    'the parsed entries. themeList() projects this into the theme.list envelope; themeAdd() ' +
    'resolves a slug against it. Returns synchronously.',
  importPath: '@astryxdesign/cli/api',
  signature: 'listThemes(): BundledTheme[]',
  keywords: ['theme', 'themes', 'manifest', 'bundled', 'adapter', 'list'],
  params: [],
  returns: [
    {
      type: 'BundledTheme[]',
      description:
        'The parsed themes array from the bundle manifest: each BundledTheme has slug, displayName, description, maintained, entry, exportName, and files. Empty when the manifest has not been generated.',
    },
  ],
  throws: [
    {
      code: 'ERR_NO_SOURCE',
      when: 'the manifest file exists but cannot be read or parsed as JSON',
    },
  ],
  examples: [
    {label: 'Read the manifest', code: 'const themes = listThemes();'},
  ],
  related: ['themeList', 'themeAdd', 'themeBuild'],
};
