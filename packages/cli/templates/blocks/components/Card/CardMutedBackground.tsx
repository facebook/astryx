'use client';

import {XDSCard} from '@xds/core/Card';

export default function CardMutedBackground() {
  return (
    <XDSCard variant="muted" width={300}>
      <p>De-emphasised card with wash background</p>
    </XDSCard>
  );
}
