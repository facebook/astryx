/** @type {import('../../../../../core/src/docs-types').TemplateDoc} */
export const doc = {
  type: 'block',
  name: 'Avatar — Fallback Chain',
  description:
    'What happens when an image fails to load. The avatar tries the primary image first, then a backup image, then initials, then a generic person icon.',
  isReady: true,
  aspectRatio: 4 / 3,
  componentsUsed: ['Avatar'],
};
