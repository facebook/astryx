/** @type {import('../../../../../core/src/docs-types').TemplateDoc} */
export const doc = {
  type: 'block',
  name: 'ChatTokenizedText — Plain Text',
  description: 'Message with no token definitions — renders as plain text. ChatTokenizedText gracefully degrades when no tokens are provided, so it can be used unconditionally on all messages.',
  isReady: true,
  aspectRatio: 16 / 9,
  componentsUsed: ['ChatTokenizedText', 'Chat', 'Layout', 'Text'],
};
