/** @type {import('../../../../../core/src/docs-types').TemplateDoc} */
export const doc = {
  type: 'block',
  name: 'ChatTokenizedText — Single Token',
  description: 'A message with one @mention token resolved to a display name. The simplest tokenized text usage — pass a single token definition and the matching pattern is replaced inline.',
  isReady: true,
  aspectRatio: 16 / 9,
  componentsUsed: ['ChatTokenizedText', 'Chat', 'Layout', 'Text'],
};
