// Copyright (c) Meta Platforms, Inc. and affiliates.

/** @type {import('@astryxdesign/cli/authoring').TemplateDoc} */
export const doc = {
  type: 'block',
  exampleFor: 'Selector',
  name: 'Selector — Option descriptions',
  displayName: 'Selector — Option descriptions',
  description:
    'Options carry a description, so the dropdown draws a two-line row. The closed trigger stays one line at the size token; renderValue is the opt-in for showing the description there too, and inside an InputGroup it folds back onto one line so the row keeps its height.',
  isReady: true,
  aspectRatio: 16 / 9,
  componentsUsed: ['Selector', 'SelectorOption', 'InputGroup', 'Button', 'Stack'],
};
